[package]
name = "ferrox-cqrs"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
ferrox-errors = { path = "../ferrox-errors" }
tracing = "0.1"
