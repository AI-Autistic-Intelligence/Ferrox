[package]
name = "ferrox-graphql"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Enterprise module ferrox-graphql for Ferrox Framework."
[dependencies]
async-graphql = { workspace = true, features = ["tokio"] }
tokio = { workspace = true, features = ["full"] }
ferrox-database-core = { version = "0.1.0", path = "../database/ferrox-database-core"  }
ferrox-errors = { version = "0.1.0", path = "../ferrox-errors"  }

[dev-dependencies]
serde_json = "1.0"
